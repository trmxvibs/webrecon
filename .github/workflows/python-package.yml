- name: Install dependencies
  run: |
    python -m pip install --upgrade pip
    python -m pip install flake8==4.0.1 pytest==7.1.2
    if [ -f requirements.txt ]; then pip install -r requirements.txt; fi
